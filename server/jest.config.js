// TP Copilot Prompt: "how can I set up testing with jest?"

module.exports = {
    testEnvironment: 'node', // Use the Node.js environment for backend testing
    testTimeout: 30000, // Optional: Increase timeout for long-running tests
  };